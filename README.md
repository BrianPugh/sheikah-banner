Simple script to convert text to rendered sheikah text.

Installation:

```
poetry install
```

Example usage:

```
poetry run python main.py --vertical --mode=1 --size=24 --padding=4 "hello world"
```
